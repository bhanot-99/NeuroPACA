.venv/
.env
__pycache__/
*.pyc
*.wav

# Piper voice model — a 63.5MB binary weight file, downloaded not
# committed. See ARCHITECTURE.md's Step 7 setup section for the source
# and the exact download step.
piper_voices/
