"""Vendored + scanner-generated bindings for cosmic-comp Wayland protocols
(B2.5b, D-9). Source XML in `xml/`; regenerate with `scan.py`. Bundled deps
(wayland, ext-foreign-toplevel-list, ext-workspace) are imported from
`pywayland.protocol.*` — only the two cosmic-specific modules are vendored.
"""
